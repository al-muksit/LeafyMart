<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:windowSoftInputMode="adjustResize"
        android:icon="@mipmap/ic_launcher"
        android:label="LeatyMart"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.LeafyMart"
        android:forceDarkAllowed="false"
        tools:targetApi="31">
        <activity
            android:name=".Activities.MainActivity"
            android:windowSoftInputMode="adjustPan|stateHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

    <!-- Line 15: android:forceDarkAllowed="false"
     for using same theme for light and dark mode

      line 19: for keypad issue solution the pop up the navbar with keyboard-->